import { AIStudioView } from "@/components/ai/ai-studio-view";

export const metadata = {
  title: "AI Writing Studio — Writely",
  description:
    "Refine cadence, rewrite tones, brainstorm character arcs, and polish grammar with specialized writer AI tools.",
};

export default function AIStudioPage() {
  return <AIStudioView />;
}
